{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "k8up.serviceAccountName" . }}
  namespace: {{ include "k8up.namespace" . }}
  labels:
{{ include "k8up.labels" . | indent 4 }}
{{- with .Values.serviceAccount.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
{{- end }}

{{- end -}}
