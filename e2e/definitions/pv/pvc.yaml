kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: subject-pvc
  namespace: k8up-e2e-subject
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeMode: Filesystem
